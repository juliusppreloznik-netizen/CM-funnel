/**
 * Minimal toast notification container.
 * Replace with a full implementation (e.g. sonner) when needed.
 */
export function Toaster() {
  return <div id="toast-container" aria-live="polite" aria-atomic="true" />;
}
